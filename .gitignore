# Miscellaneous
*.class
*.log
*.pyc
*.swp
.DS_Store
.atom/
.build/
.buildlog/
.history
.svn/
.swiftpm/
migrate_working_dir/

# IntelliJ related
*.iml
*.ipr
*.iws
.idea/

# The .vscode folder contains launch configuration and tasks you configure in
# VS Code which you may wish to be included in version control, so this line
# is commented out by default.
#.vscode/

# Flutter/Dart/Pub related
**/doc/api/
**/ios/Flutter/.last_build_id
.dart_tool/
.flutter-plugins-dependencies
.pub-cache/
.pub/
/build/
/coverage/

# Symbolication related
app.*.symbols

# Obfuscation related
app.*.map.json

# Android Studio will place build artifacts here
/android/app/debug
/android/app/profile
/android/app/release

# Generated files from build_runner - regenerated via `dart run build_runner build`
# Keep generated files committed so CI builds work without running build_runner
# *.g.dart

# Build outputs and installers
/Output/

# Test certificates and keys (DO NOT COMMIT)
*.p12
*.key
*.crt
*.csr
keychain_password.txt
test_certificate.*
test_private_key.*
test-signing.keychain*

# Secrets (DO NOT COMMIT)
lib/config/secrets.dart

# Reference data used to derive lookup tables during development, not a
# runtime asset — see the doc comment above _getReaperRootNote in
# lib/services/metadata_extractor.dart
ZD-complete.reascale