name: daw_project_manager
description: "A new Flutter project."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 0.0.0+0

environment:
  sdk: ^3.9.2

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8
  flutter_riverpod: ^3.0.3
  hive_ce: ^2.0.0
  hive_ce_flutter: ^2.3.4
  file_picker: ^11.0.2
  path: ^1.9.0
  data_table_2: ^2.5.15
  uuid: ^4.5.1
  intl: ^0.20.2
  trina_grid: ^2.2.1
  window_manager: ^0.5.1
  tray_manager: ^0.5.3
  url_launcher: ^6.0.0
  xml: ^6.6.1
  path_provider: ^2.1.1
  archive: ^4.0.7
  audioplayers: ^6.0.0
  just_audio: ^0.9.42
  just_audio_background: ^0.0.1-beta.12
  audio_service: ^0.18.15
  desktop_drop: ^0.7.0
  # Canonical-maintained typed wrapper around org.freedesktop.portal.*,
  # used by lib/services/linux_portal_notifier.dart to show notifications
  # via the portal instead of flutter_local_notifications' Linux backend
  # (which talks to org.freedesktop.Notifications directly, bypassing the
  # portal — see flatpak/README.md). Linux-only package, but fine as a
  # regular (non-platform-gated) dependency the same way plenty of other
  # Linux-only plugins here already are.
  xdg_desktop_portal: ^0.1.14
  google_sign_in: ^7.2.0
  google_sign_in_platform_interface: ^3.1.0
  googleapis: ^16.0.0
  googleapis_auth: ^2.0.0
  http: ^1.2.2
  oauth2: ^2.0.3
  crypto: ^3.0.5
  share_plus: ^12.0.2
  flutter_secure_storage: ^10.0.0
  flutter_local_notifications: ^22.0.0
  timezone: ^0.11.0
  macos_secure_bookmarks: ^0.2.1
  fl_chart: ^1.2.0
  windows_taskbar: ^1.1.2
  super_drag_and_drop: ^0.9.1
  win32: ^5.15.0
  ffi: ^2.1.4
  launch_at_startup: ^0.5.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  msix: ^3.16.12
  build_runner: ^2.4.0
  hive_ce_generator: ^1.0.0
  flutter_launcher_icons: ^0.14.4
  flutter_native_splash: ^2.4.6
  # test/services/linux_portal_notifier_test.dart spins up a private D-Bus
  # test server/fake portal object directly — xdg_desktop_portal (a
  # regular dependency) pulls this in transitively too, but the test
  # imports it directly so it should be listed, not just riding along.
  dbus: ^0.7.14

dependency_overrides:
  yaml: ^3.0.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:
  generate: true  # Enable code generation for l10n

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  assets:
    - app_icon.png
    - app_icon.ico
    - LICENSE
    - resources/daw/logos/
    - resources/tools/ffmpeg.exe

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package

msix_config:
  display_name: DAW Project Manager
  publisher_display_name: BandPass Records
  identity_name: BandPassRecords.DAWProjectManager
  msix_version: 0.0.0.0
  logo_path: windows/msix_logo.png
  capabilities: "internetClient"
  install_certificate: false
  publisher: "CN=346F9F1A-9A2E-45E9-81C1-27476C725076"
  store: true

flutter_launcher_icons:
  android: true
  ios: true
  remove_alpha_ios: true
  image_path: "app_icon.png"
  adaptive_icon_background: "#FF6100"
  adaptive_icon_foreground: "app_icon.png"
  min_sdk_android: 21 # android min sdk min:16, default 21

flutter_native_splash:
  color: "#1E1F22" # matches classicDarkTheme's default background
  image: "app_icon.png"
  android_12:
    color: "#1E1F22"
    image: "app_icon.png"
  android: true
  ios: false
  web: false